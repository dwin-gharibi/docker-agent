# Everything here is derived data from the user's local session database:
# never commit session content, summaries, judgments or logs.
session-copy.db
candidates.json
selected.json
results/
judgments/
*.log
compaction
bench-compaction
