# Example: ChatGPT account (subscription) provider
#
# Use your ChatGPT Plus/Pro/Business subscription instead of an
# OPENAI_API_KEY. Sign in once with:
#
#   docker agent auth login chatgpt
#
# The browser sign-in stores an OAuth credential that docker-agent refreshes
# automatically; check it anytime with `docker agent auth status` or
# `docker agent doctor`. Requests go to the ChatGPT Codex backend, which
# serves the gpt-5 model family over the Responses API.
#
# Run with:
#
#   docker agent run examples/chatgpt.yaml

models:
  gpt:
    provider: chatgpt
    model: gpt-5.2
    thinking_budget: medium

agents:
  root:
    model: gpt
    description: A helpful assistant running on your ChatGPT subscription
    instruction: |
      You are a helpful assistant. Answer concisely.
